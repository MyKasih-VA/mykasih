---
phase: 07-uat-fixes-pdpa-audit-final-qa
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mykasih-crm/__tests__/api/analytics-summary.test.ts
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - "A test file exists that verifies analytics summary route filters is_test=true rows"
  artifacts:
    - path: "mykasih-crm/__tests__/api/analytics-summary.test.ts"
      provides: "Test stub for analytics summary is_test filtering"
      contains: "is_test"
  key_links:
    - from: "mykasih-crm/__tests__/api/analytics-summary.test.ts"
      to: "mykasih-crm/app/api/analytics/summary/route.ts"
      via: "Jest test importing route handler"
      pattern: "analytics/summary"
---

<objective>
Wave 0: Create the missing test stub for analytics summary is_test filtering. The ic-mask.test.ts already exists with real tests from Phase 2 -- no stub needed.

Purpose: Nyquist rule requires automated verification for SEC-04 (test data exclusion from analytics). This stub enables Plan 03 Task 2 verification.
Output: Test stub file ready for Plan 03 executor to fill in or verify against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uat-fixes-pdpa-audit-final-qa/07-CONTEXT.md

Note: ic-mask.test.ts already exists at mykasih-crm/__tests__/lib/ic-mask.test.ts with 5 real tests covering plain, dashed, short, non-numeric, and empty inputs. No Wave 0 work needed for SEC-01 test coverage.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics summary test stub for is_test filtering</name>
  <files>mykasih-crm/__tests__/api/analytics-summary.test.ts</files>
  <read_first>
    - mykasih-crm/__tests__/api/webhook-voice.test.ts (existing test pattern for API route tests)
    - mykasih-crm/app/api/analytics/summary/route.ts (route to be tested)
    - mykasih-crm/jest.config.ts (test config and module aliases)
  </read_first>
  <action>
Create `mykasih-crm/__tests__/api/analytics-summary.test.ts` with test stubs that verify SEC-04 behavior:

```typescript
// Test stubs for analytics summary is_test filtering (SEC-04)
// These tests verify that all analytics queries exclude is_test=true rows

describe('GET /api/analytics/summary (SEC-04)', () => {
  test.todo('all Supabase queries include .eq("is_test", false) filter')
  test.todo('response does not include data from is_test=true call records')
  test.todo('totalCalls count excludes test calls')
  test.todo('CSAT average excludes test call ratings')
})
```

Follow the same import and mock pattern as existing test files in `__tests__/api/`. Use the project's Jest config (next/jest with jsdom environment, `@/` module alias).

The stub uses `test.todo()` so the test suite passes (Jest skips todo tests) while documenting what Plan 03 should verify during the SEC-04 audit.
  </action>
  <verify>
    <automated>cd C:/Users/navie/MyKasih/mykasih-crm && npx jest --testPathPattern="analytics-summary" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>analytics-summary.test.ts exists with todo stubs for is_test filtering verification, Jest suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 creates test stubs only -- no production code or data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00 | N/A | Test stubs | accept | No security surface -- test files only |
</threat_model>

<verification>
1. `mykasih-crm/__tests__/api/analytics-summary.test.ts` exists
2. `npx jest --passWithNoTests` passes (todo tests are skipped, not failures)
</verification>

<success_criteria>
Wave 0 complete: all test stubs required by VALIDATION.md exist. ic-mask.test.ts already has real tests; analytics-summary.test.ts has todo stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-uat-fixes-pdpa-audit-final-qa/07-00-SUMMARY.md`
</output>
