---
phase: 07-uat-fixes-pdpa-audit-final-qa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mykasih-crm/app/api/webhook/voice/route.ts
  - mykasih-crm/app/api/webhook/chat/route.ts
  - mykasih-crm/app/api/analytics/summary/route.ts
  - mykasih-crm/app/api/analytics/charts/route.ts
  - mykasih-crm/app/api/export/calls/route.ts
autonomous: true
requirements: [SEC-03, SEC-04]
must_haves:
  truths:
    - "ElevenLabs webhook rejects requests with invalid or missing HMAC signature when ELEVENLABS_WEBHOOK_SECRET is set"
    - "ElevenLabs webhook rejects replay attacks (timestamp > 5 minutes old) when timestamp header is present"
    - "Meta WA webhook GET verifies hub.verify_token; POST checks n8n secret when N8N_WEBHOOK_SECRET is set"
    - "All analytics and export routes filter is_test != true from query results"
    - "Testing Console voice calls carry is_test=true; chatbot simulator does not write to DB"
    - "Dev mode works: missing webhook secrets skip validation with a console warning"
  artifacts:
    - path: "mykasih-crm/app/api/webhook/voice/route.ts"
      provides: "HMAC + replay validation for ElevenLabs"
      contains: "elevenlabs-signature"
    - path: "mykasih-crm/app/api/webhook/chat/route.ts"
      provides: "Meta WA verify + n8n secret check"
      contains: "hub.verify_token"
  key_links:
    - from: "analytics/summary/route.ts"
      to: "calls table"
      via: "Supabase query with is_test filter"
      pattern: "is_test.*false"
---

<objective>
Verify and harden all webhook secret validation and test data exclusion controls. Fix any gaps found during code review.

Purpose: SEC-03 (webhook security) and SEC-04 (test data exclusion) require verification that existing controls work correctly. Research confirmed most are already implemented — this plan audits and fixes any gaps.
Output: Verified and potentially hardened webhook routes + confirmed analytics filtering
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uat-fixes-pdpa-audit-final-qa/07-CONTEXT.md
@.planning/phases/07-uat-fixes-pdpa-audit-final-qa/07-RESEARCH.md

<interfaces>
RESEARCH.md verified status:
- webhook/voice/route.ts: HMAC-SHA256 present, replay protection (5min) present, is_test extraction present
- webhook/chat/route.ts: Meta WA hub.verify_token on GET, n8n secret on POST (conditional)
- analytics/summary/route.ts: all 12 parallel queries filter .eq('is_test', false)
- analytics/charts/route.ts: main calls query uses .eq('is_test', false)
- export/calls/route.ts: uses includeTest flag, defaults to excluding test
- chatbot/simulate/route.ts: does NOT write to DB — no is_test gap
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and verify webhook secret validation (SEC-03)</name>
  <files>mykasih-crm/app/api/webhook/voice/route.ts, mykasih-crm/app/api/webhook/chat/route.ts</files>
  <read_first>
    - mykasih-crm/app/api/webhook/voice/route.ts (full file — verify HMAC + replay)
    - mykasih-crm/app/api/webhook/chat/route.ts (full file — verify Meta WA + n8n secret)
    - mykasih-crm/app/api/chatbot/message/route.ts (verify n8n secret check here too)
  </read_first>
  <action>
**Verification checklist (per D-09, D-10, D-11, D-12):**

1. **ElevenLabs webhook (voice/route.ts) — per D-09:**
   - Confirm `elevenlabs-signature` header is read and parsed for `t=<timestamp>,v0=<hmac>` format
   - Confirm HMAC-SHA256 is computed using `ELEVENLABS_WEBHOOK_SECRET` and compared to `v0` value
   - Confirm 401 response on invalid/missing signature when secret is set
   - If HMAC validation is NOT present or is incomplete: add it using Node.js `crypto.createHmac('sha256', secret).update(payload).digest('hex')` pattern

2. **Replay protection (voice/route.ts) — per D-11:**
   - Confirm `t` value from signature header is parsed as Unix timestamp
   - Confirm request is rejected if `Date.now() / 1000 - t > 300` (5 minute window)
   - If replay check is present: verify. If not present AND header provides timestamp: add it.
   - If ElevenLabs doesn't provide timestamp (research says it does): skip per D-11 guidance.

3. **Dev mode flexibility (voice/route.ts) — per D-12:**
   - Confirm that when `ELEVENLABS_WEBHOOK_SECRET` is empty/undefined, signature validation is skipped
   - Add `console.warn('[webhook/voice] ELEVENLABS_WEBHOOK_SECRET not set — signature validation skipped')` if not already present

4. **Meta WA webhook (chat/route.ts) — per D-10:**
   - Confirm GET handler checks `hub.verify_token` against `META_WA_VERIFY_TOKEN` env var
   - Confirm POST handler checks n8n secret header when `N8N_WEBHOOK_SECRET` is set
   - Confirm POST returns 200 even on errors (per Phase 3 decision: Meta retries on non-200)

5. **Dev mode flexibility (chat/route.ts) — per D-12:**
   - Confirm that when `N8N_WEBHOOK_SECRET` is empty/undefined, secret check is skipped
   - Add `console.warn('[webhook/chat] N8N_WEBHOOK_SECRET not set — secret validation skipped')` if not already present

6. **chatbot/message/route.ts:**
   - Confirm n8n secret validation is present if this route is called directly (not just via chat webhook)

**If all checks pass with no changes needed:** Document findings in the SUMMARY without modifying files. Only modify files if gaps are found.

**If gaps are found:** Fix them inline in the existing route files. Do NOT rewrite entire files — only add the missing validation logic.
  </action>
  <verify>
    <automated>cd C:/Users/navie/MyKasih/mykasih-crm && grep -c "elevenlabs-signature\|ELEVENLABS_WEBHOOK_SECRET\|createHmac" app/api/webhook/voice/route.ts && grep -c "hub.verify_token\|N8N_WEBHOOK_SECRET\|META_WA_VERIFY_TOKEN" app/api/webhook/chat/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - mykasih-crm/app/api/webhook/voice/route.ts contains `elevenlabs-signature` header read
    - mykasih-crm/app/api/webhook/voice/route.ts contains HMAC comparison logic (`createHmac` or `timingSafeEqual`)
    - mykasih-crm/app/api/webhook/voice/route.ts contains timestamp age check (`300` or `5 * 60`)
    - mykasih-crm/app/api/webhook/voice/route.ts contains conditional skip when ELEVENLABS_WEBHOOK_SECRET is falsy
    - mykasih-crm/app/api/webhook/chat/route.ts contains `hub.verify_token` check
    - mykasih-crm/app/api/webhook/chat/route.ts contains n8n secret check conditional on N8N_WEBHOOK_SECRET
  </acceptance_criteria>
  <done>All webhook routes verified: ElevenLabs HMAC + replay protection present, Meta WA verify_token + n8n secret present, dev mode flexibility preserved</done>
</task>

<task type="auto">
  <name>Task 2: Audit and verify test data exclusion from analytics (SEC-04)</name>
  <files>mykasih-crm/app/api/analytics/summary/route.ts, mykasih-crm/app/api/analytics/charts/route.ts, mykasih-crm/app/api/export/calls/route.ts</files>
  <read_first>
    - mykasih-crm/app/api/analytics/summary/route.ts (verify is_test filter on all queries)
    - mykasih-crm/app/api/analytics/charts/route.ts (verify is_test filter)
    - mykasih-crm/app/api/export/calls/route.ts (verify is_test filter)
    - mykasih-crm/app/api/chatbot/simulate/route.ts (verify no DB writes)
    - mykasih-crm/components/testing/VoiceAgentTab.tsx (verify is_test=true tagging)
  </read_first>
  <action>
**Verification checklist (per D-13, D-14):**

1. **Analytics summary route — per D-13:**
   - Read the full file and verify every Supabase `.from('calls')` query includes `.eq('is_test', false)` or equivalent
   - Research confirmed 12 parallel queries all filter — verify this count
   - If any query is missing the filter: add `.eq('is_test', false)` to the query chain

2. **Analytics charts route — per D-13:**
   - Read the full file and verify the main calls query includes `.eq('is_test', false)`
   - If missing: add the filter

3. **Export calls route — per D-13:**
   - Read the full file and verify `is_test` exclusion is applied
   - Research confirmed it uses `includeTest` flag defaulting to false — verify

4. **Chatbot simulator — per D-14:**
   - Read `app/api/chatbot/simulate/route.ts` and confirm it does NOT insert into `calls` or `transcripts` tables
   - If it does write to DB: ensure `is_test: true` is set on any inserted record
   - Research confirmed: simulate route is pure in-memory, no DB writes — verify

5. **Voice agent tab — per D-14:**
   - Read `components/testing/VoiceAgentTab.tsx` and confirm `is_test` is passed via `dynamicVariables` to the ElevenLabs session
   - Also check if the fallback POST to `/api/calls` includes `is_test: true`
   - Research confirmed dual tagging: dynamicVariables.is_test + fallback POST — verify

6. **Comprehensive calls table query audit:**
   - Search ALL route files for `.from('calls').select` or `.from('calls')` queries
   - Verify that every route that aggregates or reports on calls data (not individual call lookup) filters `is_test`
   - Individual call lookups (e.g., `calls/[id]/transcript`) do NOT need is_test filter (they show specific records)

**If all checks pass:** Document findings in SUMMARY. No file modifications needed.
**If gaps found:** Fix by adding `.eq('is_test', false)` to the missing query.
  </action>
  <verify>
    <automated>cd C:/Users/navie/MyKasih/mykasih-crm && grep -rn "is_test" app/api/analytics/ app/api/export/ 2>/dev/null | grep -c "false\|is_test"</automated>
  </verify>
  <acceptance_criteria>
    - mykasih-crm/app/api/analytics/summary/route.ts contains `is_test` filter (grep returns matches)
    - mykasih-crm/app/api/analytics/charts/route.ts contains `is_test` filter
    - mykasih-crm/app/api/export/calls/route.ts contains `is_test` filter or includeTest logic
    - mykasih-crm/app/api/chatbot/simulate/route.ts does NOT contain `.from('calls').insert` or `.from('transcripts').insert`
    - mykasih-crm/components/testing/VoiceAgentTab.tsx contains `is_test` in dynamicVariables or session config
  </acceptance_criteria>
  <done>All analytics/export routes verified to exclude is_test=true rows; Testing Console voice tab verified to tag is_test=true; chatbot simulator verified to not write to DB</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ElevenLabs → /api/webhook/voice | External service sends POST with HMAC signature |
| Meta WA → /api/webhook/chat | External service sends POST with verify token |
| Testing Console → Supabase | Test data must be tagged and excluded from analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | webhook/voice | mitigate | HMAC-SHA256 signature validation with `elevenlabs-signature` header; 401 on mismatch |
| T-07-08 | Spoofing | webhook/voice | mitigate | Replay protection: reject timestamps older than 5 minutes |
| T-07-09 | Spoofing | webhook/chat | mitigate | Meta WA hub.verify_token on GET; n8n bearer token on POST |
| T-07-10 | Repudiation | analytics routes | mitigate | is_test=false filter ensures test data cannot pollute production analytics |
| T-07-11 | Information Disclosure | analytics routes | accept | Test data visible in data pages per D-08 — acceptable, filtered only from aggregations |
</threat_model>

<verification>
1. Webhook voice route contains HMAC validation, replay protection, and dev-mode skip
2. Webhook chat route contains verify_token and n8n secret checks
3. All analytics/export routes filter is_test=false
4. Testing Console tags is_test=true on voice calls
5. Chatbot simulator does not write to DB
</verification>

<success_criteria>
SEC-03 is verified: all incoming webhooks reject requests with invalid or missing secrets.
SEC-04 is verified: all test calls are tagged is_test=true and excluded from analytics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-uat-fixes-pdpa-audit-final-qa/07-03-SUMMARY.md`
</output>
