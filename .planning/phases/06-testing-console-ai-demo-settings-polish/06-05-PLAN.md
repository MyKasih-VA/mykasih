---
phase: 06-testing-console-ai-demo-settings-polish
plan: 05
type: execute
wave: 2
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - mykasih-crm/lib/translations.ts
  - mykasih-crm/app/(dashboard)/live-monitor/page.tsx
  - mykasih-crm/app/(dashboard)/integrations/page.tsx
  - mykasih-crm/components/layout/Sidebar.tsx
  - mykasih-crm/components/layout/Topbar.tsx
autonomous: false
requirements: [TEST-06]

must_haves:
  truths:
    - "No inline language === 'en' ternaries remain in any dashboard page or component file (automated-verifiable via grep)"
    - "Language toggle relabels all sidebar items, page titles, and table headers when toggled (checkpoint-verifiable)"
    - "Every visible string on every dashboard page uses t() from translations.ts"
  artifacts:
    - path: "mykasih-crm/lib/translations.ts"
      provides: "Complete translation coverage for all dashboard pages"
      contains: "testing.title"
  key_links:
    - from: "all dashboard page.tsx files"
      to: "mykasih-crm/lib/translations.ts"
      via: "t() function calls"
      pattern: "t\\("
---

<objective>
Full language polish sweep: audit all dashboard pages for inline ternaries, replace with t() calls, add any missing translation keys, and verify complete BM/EN coverage.

Purpose: Every visible string on every dashboard page uses the t() translation function, ensuring the language toggle works perfectly across the entire application. Per D-25, D-26, D-27.
Output: Updated translations.ts with any missing keys; all page files using t() exclusively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-console-ai-demo-settings-polish/06-CONTEXT.md
@.planning/phases/06-testing-console-ai-demo-settings-polish/06-UI-SPEC.md
@.planning/phases/06-testing-console-ai-demo-settings-polish/06-02-SUMMARY.md
@.planning/phases/06-testing-console-ai-demo-settings-polish/06-03-SUMMARY.md
@.planning/phases/06-testing-console-ai-demo-settings-polish/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix inline ternaries in confirmed files</name>
  <files>mykasih-crm/lib/translations.ts, mykasih-crm/app/(dashboard)/live-monitor/page.tsx, mykasih-crm/app/(dashboard)/integrations/page.tsx, mykasih-crm/components/layout/Sidebar.tsx, mykasih-crm/components/layout/Topbar.tsx</files>
  <read_first>
    - mykasih-crm/lib/translations.ts
    - mykasih-crm/app/(dashboard)/live-monitor/page.tsx
    - mykasih-crm/app/(dashboard)/integrations/page.tsx
    - mykasih-crm/components/layout/Sidebar.tsx
    - mykasih-crm/components/layout/Topbar.tsx
  </read_first>
  <action>
Per D-25, D-26, D-27. This task has a defined primary scope and a verification-only secondary scope.

**Primary scope (files to MODIFY):** These are the files confirmed by RESEARCH.md to contain inline language ternaries:
1. `mykasih-crm/app/(dashboard)/live-monitor/page.tsx` — known inline ternaries for status labels, column headers
2. `mykasih-crm/app/(dashboard)/integrations/page.tsx` — known inline ternaries for integration card titles, status text
3. `mykasih-crm/components/layout/Sidebar.tsx` — known inline ternaries for nav item labels
4. `mykasih-crm/components/layout/Topbar.tsx` — known inline ternaries for date formatting, greeting text

For each file:
- Find every `language === 'en' ? '...' : '...'` ternary
- Determine the appropriate translation key name following the existing naming convention
- If the key already exists in translations.ts, replace the ternary with `t('existing.key', language)`
- If the key does NOT exist, add it to translations.ts with both EN and BM values, then replace the ternary

**Secondary scope (verification-only, read + grep, NO write expected):**
After fixing the primary files, run a comprehensive grep to check for any remaining ternaries:
```bash
grep -rn "language === 'en' ?" mykasih-crm/app/ mykasih-crm/components/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v ".test." | grep -v "demo/page.tsx"
```

If the grep finds additional files beyond the primary scope (e.g., analytics, knowledge-base, staff, voice-calls, chat-messages, all-interactions, tickets, beneficiaries, dashboard home, or component files in dashboard/calls/tickets directories), fix those too. But the primary scope above covers the known instances from RESEARCH.md.

**Adding missing translation keys:**
Maintain the existing grouping and comment structure in translations.ts. Each new key must have both `en` and `bm` values.

**Exceptions** (do NOT translate — per D-27):
- Dynamic data from the database (names, WA numbers, timestamps, etc.)
- Content inside the Anam AI / ElevenLabs embeds (controlled by vendor)
- Technical strings (API error messages in console.error, not shown to users)
- The `/demo` page (English only, per D-16)
  </action>
  <verify>
    <automated>cd mykasih-crm && grep -rn "language === 'en' ?" app/ components/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v ".test." | grep -v "demo/page.tsx" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no inline ternaries" || echo "FAIL: inline ternaries remain"</automated>
  </verify>
  <acceptance_criteria>
    - Running `grep -rn "language === 'en' ?" mykasih-crm/app/ mykasih-crm/components/ --include="*.tsx"` returns zero matches (excluding demo/page.tsx and test files)
    - mykasih-crm/lib/translations.ts compiles without TypeScript errors
    - All page files import `t` from `@/lib/translations`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Zero inline language ternaries remain in any dashboard page or component file. All visible strings use t() from translations.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 6 — Testing Console, Demo, Settings, Language</name>
  <files>mykasih-crm/app/(dashboard)/testing/page.tsx, mykasih-crm/app/demo/page.tsx, mykasih-crm/app/(dashboard)/settings/page.tsx</files>
  <action>Human visually verifies all Phase 6 deliverables across Testing Console, AI Demo, Settings, and language toggle.</action>
  <what-built>Complete Phase 6: Testing Console (3 tabs), AI Demo page, Settings page, and full BM/EN language coverage across all dashboard pages.</what-built>
  <how-to-verify>
    1. Navigate to /testing — verify 3 tabs exist: Voice Agent, Kasih Chatbot, Anam AI
    2. Click "Start Session" on Voice Agent tab — verify status badge changes, mic button toggles to "End Session"
    3. Switch to Kasih Chatbot tab — type a message, verify bot response appears with intent badge
    4. Click "Clear Conversation" — verify chat resets
    5. Switch to Anam AI tab — verify embed loads
    6. Open /demo in incognito/private window (no login) — verify page loads with MyKasih logo, "AI Helpline Demo" title, Anam AI embed, and "Powered by Anam AI" footer. No sidebar visible.
    7. Navigate to /settings — verify 4 cards (Agent Hours, Notifications, Data Retention, Webhook URLs)
    8. Change agent hours start to "09:00" and click Save Changes — verify success toast
    9. Copy a webhook URL — verify clipboard contains the URL
    10. Toggle language from EN to BM in the topbar — verify ALL sidebar labels, page titles, and button labels switch to Bahasa Malaysia
    11. Navigate through Dashboard, Voice Calls, Tickets, Analytics, KB, Staff, Integrations, Live Monitor — verify NO English text remains when in BM mode (except dynamic data)
  </how-to-verify>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>Phase 6 fully verified: Testing Console works, Demo page loads publicly, Settings save correctly, and language toggle covers all pages.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Language polish is UI-only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | Translation keys | accept | Translation keys are compile-time constants. The t() function returns hardcoded strings — no user input flows into translations. No XSS risk. |
</threat_model>

<verification>
- `grep -rn "language === 'en' ?" app/ components/ --include="*.tsx"` returns zero matches (excluding demo and tests)
- `npx tsc --noEmit` passes
- `npm test` passes
- Language toggle works on every dashboard page (checkpoint-verified)
</verification>

<success_criteria>
Every visible string on every dashboard page uses t() from translations.ts. No inline ternaries remain (grep-verified). Switching the language toggle instantly relabels all UI elements (checkpoint-verified). Full BM/EN coverage is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-console-ai-demo-settings-polish/06-05-SUMMARY.md`
</output>
