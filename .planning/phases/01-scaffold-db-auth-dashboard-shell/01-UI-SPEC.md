---
phase: 1
slug: scaffold-db-auth-dashboard-shell
status: draft
shadcn_initialized: true
preset: default (slate base, CSS variables, RSC, TSX)
created: 2026-04-11
revised: 2026-04-11
revision_reason: Fix sidebar nav icon margin-right from 10px to 8px (spacing scale compliance); add focal point statements for login page and dashboard home
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Scaffold, DB, Auth & Dashboard Shell.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn | components.json confirmed |
| Preset | default — slate base, cssVariables: true, RSC: true | mykasih-crm/components.json |
| Component library | Radix UI (via shadcn) | node_modules present |
| Icon library | Lucide React | CONTEXT.md — node_modules present |
| Font — primary | Inter (Google Fonts, weights 400 + 600) | CLAUDE.md |
| Font — monospace | JetBrains Mono (Google Fonts, weight 400) | CLAUDE.md — code/numbers only |
| Theme mode | Dark only — no light mode toggle | CLAUDE.md |
| Tailwind version | 4 (`@import "tailwindcss"` syntax) | globals.css confirmed |
| CSS variable approach | All colors via custom CSS vars — zero hardcoded hex in components | CLAUDE.md |

---

## globals.css Rewrite Contract

The current `globals.css` uses Geist font and only 2 color tokens. This file MUST be fully replaced with the following token set before any component work begins.

```css
@import "tailwindcss";

@theme inline {
  --font-sans: var(--font-inter);
  --font-mono: var(--font-jetbrains-mono);
}

:root {
  /* Surface */
  --bg-primary:     #0D1117;
  --bg-surface:     #161B22;
  --bg-border:      #21262D;

  /* Accent */
  --accent-primary: #2E7D32;
  --accent-teal:    #00897B;

  /* Text */
  --text-primary:   #E6EDF3;
  --text-muted:     #7D8590;

  /* Status */
  --status-green:   #3FB950;
  --status-yellow:  #D29922;
  --status-red:     #F85149;

  /* Charts / Channel */
  --chart-voice:    #43A047;
  --chart-chat:     #00897B;
}

body {
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-sans);
}
```

All 12 color tokens are required (INFRA-03). No token may be omitted.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage in This Phase |
|-------|-------|---------------------|
| xs | 4px | Icon-to-label gap, inline badge padding |
| sm | 8px | Input internal padding, avatar-to-name gap |
| md | 16px | Card internal padding, form field spacing |
| lg | 24px | Sidebar section gap, stat card gap |
| xl | 32px | Page content top padding, chart outer margin |
| 2xl | 48px | Major section breaks within dashboard home |
| 3xl | 64px | Reserved — not used in Phase 1 |

Exceptions:
- Sidebar width: 260px fixed (DASH-01 — not a spacing token, structural)
- Touch/click targets: minimum 44px height on all interactive sidebar items and topbar controls
- Sidebar nav item height: 44px (accessibility minimum, not a spacing token)

---

## Typography

All text uses Inter. JetBrains Mono is used exclusively for IC numbers displayed in masked format.

Font weight palette: 400 (Regular) and 600 (SemiBold) only. No other weights.

| Role | Size | Weight | Line Height | CSS Class Pattern | Usage |
|------|------|--------|-------------|-------------------|-------|
| Display | 28px | 600 (SemiBold) | 1.2 | text-[28px] font-semibold | Stat card numbers |
| Heading | 16px | 600 (SemiBold) | 1.3 | text-base font-semibold | Page title in topbar, card titles, section labels, nav group headers |
| Body | 14px | 400 (Regular) | 1.5 | text-sm font-normal | Table rows, sidebar nav items, form labels |
| Caption | 12px | 400 (Regular) | 1.4 | text-xs font-normal | Metadata, timestamps, muted descriptions, code labels, nav group labels, badge text, axis labels |

Size consolidation notes:
- 20px merged into 16px SemiBold — 16px at SemiBold weight provides sufficient visual hierarchy for page headings and card titles
- 13px merged into 12px — captions, code labels, and user name/logout text all use 12px Regular
- 11px merged into 12px — nav group labels, badge text, and chart axis labels all use 12px Regular (or 12px SemiBold for active badge text)
- 700 Bold removed — 28px SemiBold (600) provides sufficient visual weight for stat card numbers; no Bold needed

---

## Color

### 60 / 30 / 10 Split

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--bg-primary` | #0D1117 | Page background, login page background |
| Secondary (30%) | `--bg-surface` | #161B22 | Sidebar, cards, panels, topbar, modals, table rows (hover) |
| Accent (10%) | `--accent-primary` | #2E7D32 | Active nav item highlight, primary buttons, active state indicators |
| Accent secondary | `--accent-teal` | #00897B | Secondary action buttons, links, focus rings |
| Destructive | `--status-red` | #F85149 | Logout confirmation only (no destructive actions in Phase 1 data) |

Accent (`--accent-primary`) is reserved for exactly these elements — no others:
1. Active sidebar navigation item background + left border indicator
2. Primary CTA button background (Sign In button on login page)
3. "AI Connected" status dot in sidebar footer
4. Stat card delta positive trend indicator

### Text Colors

| Token | Hex | Usage |
|-------|-----|-------|
| `--text-primary` | #E6EDF3 | All body text, table content, headings |
| `--text-muted` | #7D8590 | Captions, timestamps, nav group labels, placeholder text |

### Border Color

| Token | Hex | Usage |
|-------|-----|-------|
| `--bg-border` | #21262D | All dividers, input borders, card borders, table row separators |

### Status Colors (Phase 1 usage)

| Token | Hex | Usage in Phase 1 |
|-------|-----|------------------|
| `--status-green` | #3FB950 | "AI Connected" dot in sidebar footer; resolved outcome badge |
| `--status-yellow` | #D29922 | Pending/in-progress badge; warning indicators |
| `--status-red` | #F85149 | Error message below login form (AUTH-03) |

### Channel Badge Colors (Recent Interactions table)

| Channel | Token | Hex | Icon |
|---------|-------|-----|------|
| Voice | `--chart-voice` | #43A047 | Phone icon (Lucide `Phone`) |
| Chat | `--chart-chat` | #00897B | Message icon (Lucide `MessageSquare`) |

---

## Component Specifications

### Login Page (`/login`)

Focal point: the centered sign-in card — the logo, app name, and Sign In button are the only visual elements drawing the eye on an otherwise empty dark background.

- Full-page centered layout on `--bg-primary` background
- Card container: `--bg-surface`, 1px border `--bg-border`, border-radius 12px, padding 32px, max-width 400px
- MyKasih logo centered above card: `https://mykasih.com.my/wp-content/uploads/2025/05/MyKasih-logo.png`, width 140px
- App name: "MyKasih Command Centre" — 16px SemiBold, `--text-primary`, centered, below logo, margin-top 16px
- Subtitle: "AI Helpline CRM v1.0" — 12px Regular, `--text-muted`, centered, margin-top 4px
- Email input: full width, height 44px, `--bg-border` border, `--bg-primary` background, `--text-primary`, placeholder `--text-muted`
- Password input: same spec as email input
- Sign In button: full width, height 44px, `--accent-primary` background, `--text-primary`, 14px SemiBold
- Error message (AUTH-03): 12px Regular, `--status-red`, displayed directly below Sign In button, margin-top 8px
- No "Forgot password" link in Phase 1 (out of scope)

### Sidebar (`/components/layout/Sidebar.tsx`)

- Fixed position, width 260px, height 100vh, background `--bg-surface`, right border 1px `--bg-border`
- Logo area: top 16px padding, MyKasih logo 32px height + "MyKasih Command Centre" 14px SemiBold beside it
- Nav group label: 12px uppercase, `--text-muted`, letter-spacing 0.08em, padding 8px 16px, margin-top 16px
- Nav item: height 44px, padding 0 16px, 14px Regular, `--text-primary`, border-radius 6px (inside sidebar)
- Nav item active state: background `--accent-primary` at 15% opacity, left border 3px solid `--accent-primary`, text `--accent-primary`
- Nav item hover state: background `--bg-border` at 60% opacity
- Nav icon: 16px Lucide icon, margin-right 8px, color inherits from nav item state
- Sidebar footer: padding 16px, border-top 1px `--bg-border`
  - User avatar: 32px circle, initials fallback
  - User name: 12px SemiBold, `--text-primary`
  - Role badge: 12px, background `--bg-border`, `--text-muted`, border-radius 4px, padding 4px 8px
  - "AI Connected" indicator: 8px dot `--status-green` + "AI Connected" 12px `--text-muted`, margin-top 4px
  - Logout: 12px `--text-muted`, hover `--status-red`, margin-top 8px

#### Navigation Structure (all 14 items, 5 groups — DASH-01)

```
OVERVIEW
  Dashboard (LayoutDashboard icon)

CALL MANAGEMENT
  Voice Calls (Phone icon)
  Chat Messages (MessageSquare icon)
  All Interactions (Activity icon)

OPERATIONS
  Tickets (Ticket icon)
  Beneficiaries (Users icon)
  Live Monitor (Radio icon)

INTELLIGENCE
  Analytics (BarChart2 icon)

SYSTEM
  Knowledge Base (BookOpen icon)
  Staff Management (UserCog icon)
  Integrations (Plug icon)
  Testing Console (FlaskConical icon)
  AI Demo (Sparkles icon)
  Settings (Settings icon)
```

### Topbar (`/components/layout/Topbar.tsx`)

- Height 64px, background `--bg-surface`, border-bottom 1px `--bg-border`, padding 0 24px
- Left: dynamic page title — 16px SemiBold, `--text-primary`
- Left (below title): date display — 12px Regular, `--text-muted` (format: "Friday, 11 April 2026" in EN / "Jumaat, 11 April 2026" in BM)
- Right group (left to right): EN/BM language toggle | search input | notification bell | user avatar dropdown
- Language toggle: pill toggle, "EN" / "BM", active side has `--accent-primary` background, 12px SemiBold
- Search input: width 220px, height 36px, `--bg-primary` background, `--bg-border` border, 12px placeholder "Search..."
- Notification bell: 20px Lucide `Bell` icon, `--text-muted`, hover `--text-primary`, 44px tap target, `aria-label="Notifications"` (EN) / `aria-label="Pemberitahuan"` (BM)
- User avatar: 32px circle with initials fallback, dropdown on click (show name, role, logout)

### Dashboard Home — Stat Cards (DASH-05)

Focal point: the four stat cards row is the primary data surface — large 28px numbers draw the eye before any chart or table on the page.

Four cards in a 4-column grid (responsive: 2-col on tablet, 1-col on mobile).

| Card | Label (EN) | Label (BM) | Metric | Sub-info |
|------|-----------|-----------|--------|----------|
| 1 | Today's Interactions | Interaksi Hari Ini | count (voice + chat) | voice count + chat count below |
| 2 | Resolution Rate | Kadar Penyelesaian | percentage % | vs. yesterday delta |
| 3 | Open Tickets | Tiket Terbuka | count | count in-progress below |
| 4 | Avg. Duration | Purata Tempoh | seconds (voice) | message count (chat) below |

Stat card anatomy:
- Background: `--bg-surface`, border 1px `--bg-border`, border-radius 8px, padding 20px
- Label: 12px Regular, `--text-muted`, margin-bottom 8px
- Primary metric: 28px SemiBold, `--text-primary`
- Sub-info: 12px Regular, `--text-muted`, margin-top 4px
- Loading state: Shadcn `Skeleton` — 16px height for label, 36px height for metric, 12px height for sub-info

### Dashboard Home — Stacked Bar Chart (DASH-06)

- Component: Recharts `BarChart` with `Bar` (stacked)
- Data: last 7 days, one bar per day
- Voice stack: fill `--chart-voice` (#43A047)
- Chat stack: fill `--chart-chat` (#00897B)
- Background: `--bg-surface`, border 1px `--bg-border`, border-radius 8px, padding 20px
- Chart title: "Call Volume — Last 7 Days" (EN) / "Jumlah Panggilan — 7 Hari Lepas" (BM), 16px SemiBold
- Legend: inline below title, voice dot + label, chat dot + label, 12px Regular `--text-muted`
- X-axis: day labels (Mon, Tue...), 12px `--text-muted`
- Y-axis: count labels, 12px `--text-muted`
- Tooltip: `--bg-surface` background, `--bg-border` border, 12px text
- Loading state: Shadcn `Skeleton` full chart area height (240px)

### Dashboard Home — Donut Chart (DASH-07)

- Component: Recharts `PieChart` with `Pie` (innerRadius set for donut)
- Data: call categories — eligibility, faq, registration, complaint, merchant_lookup, balance_check
- Color sequence: use 6 distinct values from `--accent-primary`, `--accent-teal`, `--status-green`, `--status-yellow`, `--chart-voice`, `--chart-chat`
- Background: `--bg-surface`, border 1px `--bg-border`, border-radius 8px, padding 20px
- Chart title: "Interactions by Category" (EN) / "Interaksi Mengikut Kategori" (BM), 16px SemiBold
- Legend: right side, category label + count + percentage, 12px Regular
- Loading state: Shadcn `Skeleton` circular 160px

### Dashboard Home — Recent Interactions Table (DASH-08)

Columns: Channel | Caller | Category | Outcome | Time | Duration/Messages

| Column | Width | Content |
|--------|-------|---------|
| Channel | 72px | Badge: voice (Phone icon, `--chart-voice` bg) or chat (MessageSquare icon, `--chart-chat` bg) |
| Caller | auto | `caller_name` — 14px Regular |
| Category | 140px | Category label — 12px, `--text-muted` |
| Outcome | 100px | Status badge — resolved (`--status-green`), escalated (`--status-red`), pending (`--status-yellow`) |
| Time | 100px | Relative timestamp — "2h ago" — 12px `--text-muted` |
| Duration/Msgs | 90px | Voice: seconds (e.g. "142s") / Chat: message count (e.g. "6 msgs") — 12px |

- Row height: 48px
- Row hover: background `--bg-border` at 40% opacity
- Row click: opens transcript modal (modal spec in Phase 4 — placeholder only in Phase 1)
- Background: `--bg-surface`, border 1px `--bg-border`, border-radius 8px
- Table title: "Recent Interactions" (EN) / "Interaksi Terkini" (BM), 16px SemiBold, padding 20px bottom 12px
- Loading state: 10 rows of Shadcn `Skeleton` at 48px height each
- Empty state: centered in table area — icon (Lucide `Inbox`, 32px, `--text-muted`) + heading + body (see Copywriting Contract)

### Channel Badge Component (`/components/calls/ChannelBadge.tsx`)

Used in Recent Interactions table and all future pages:

```
Voice badge: background --chart-voice at 15% opacity, border 1px --chart-voice at 40% opacity,
             text --chart-voice, Phone icon 12px + "Voice" text, 12px SemiBold, padding 4px 8px, border-radius 4px

Chat badge:  background --chart-chat at 15% opacity, border 1px --chart-chat at 40% opacity,
             text --chart-chat, MessageSquare icon 12px + "Chat" text, 12px SemiBold, padding 4px 8px, border-radius 4px
```

### Outcome Badge

```
resolved:   background --status-green at 12% opacity, text --status-green, 12px SemiBold, padding 4px 8px
escalated:  background --status-red at 12% opacity, text --status-red, 12px SemiBold, padding 4px 8px
pending:    background --status-yellow at 12% opacity, text --status-yellow, 12px SemiBold, padding 4px 8px
callback:   background --accent-teal at 12% opacity, text --accent-teal, 12px SemiBold, padding 4px 8px
abandoned:  background --text-muted at 12% opacity, text --text-muted, 12px SemiBold, padding 4px 8px
```

### Loading Skeletons (DASH-09)

All skeletons use Shadcn `Skeleton` component:
- Pulse animation (default Shadcn behavior)
- Background: `--bg-border` (overrides Shadcn default if needed via className)
- Stat card skeleton: 3 skeleton bars stacked (label 16px h, metric 36px h, sub 12px h)
- Table row skeleton: full-width bar at 24px h, repeating 10 times with 12px gap
- Chart skeleton: single rectangle at full width, 240px height

---

## Copywriting Contract

### Login Page

| Element | EN Copy | BM Copy |
|---------|---------|---------|
| Email placeholder | Email address | Alamat e-mel |
| Password placeholder | Password | Kata laluan |
| Primary CTA | Sign In | Log Masuk |
| Error — invalid credentials | Invalid email or password. Please try again. | E-mel atau kata laluan tidak sah. Sila cuba lagi. |
| Error — server error | Something went wrong. Please try again later. | Ralat berlaku. Sila cuba sebentar lagi. |

### Dashboard Home

| Element | EN Copy | BM Copy |
|---------|---------|---------|
| Page title | Dashboard | Papan Pemuka |
| Stat card 1 label | Today's Interactions | Interaksi Hari Ini |
| Stat card 2 label | Resolution Rate | Kadar Penyelesaian |
| Stat card 3 label | Open Tickets | Tiket Terbuka |
| Stat card 4 label | Avg. Duration | Purata Tempoh |
| Recent table title | Recent Interactions | Interaksi Terkini |
| Chart 1 title | Call Volume — Last 7 Days | Jumlah Panggilan — 7 Hari Lepas |
| Chart 2 title | Interactions by Category | Interaksi Mengikut Kategori |

### Empty States

| Location | EN Heading | EN Body | BM Heading | BM Body |
|----------|-----------|---------|-----------|---------|
| Recent Interactions | No interactions yet | Interactions will appear here once calls or chats are received. | Tiada interaksi lagi | Interaksi akan dipaparkan di sini setelah panggilan atau sembang diterima. |
| Stat cards (zero state) | 0 | — (just the number, no body) | 0 | — |

### Sidebar Footer

| Element | EN | BM |
|---------|----|----|
| AI status | AI Connected | AI Disambung |
| Logout | Sign Out | Log Keluar |

### Navigation Labels (EN / BM pairs)

| Nav Item | EN | BM |
|----------|----|----|
| Dashboard | Dashboard | Papan Pemuka |
| Voice Calls | Voice Calls | Panggilan Suara |
| Chat Messages | Chat Messages | Mesej Chat |
| All Interactions | All Interactions | Semua Interaksi |
| Tickets | Tickets | Tiket |
| Beneficiaries | Beneficiaries | Penerima Manfaat |
| Live Monitor | Live Monitor | Monitor Langsung |
| Analytics | Analytics | Analitik |
| Knowledge Base | Knowledge Base | Pangkalan Ilmu |
| Staff Management | Staff Management | Pengurusan Kakitangan |
| Integrations | Integrations | Integrasi |
| Testing Console | Testing Console | Konsol Ujian |
| AI Demo | AI Demo | Demo AI |
| Settings | Settings | Tetapan |

### Destructive Actions

No destructive data actions in Phase 1. The only destructive-adjacent action is Sign Out (session termination).

| Action | Confirmation approach |
|--------|----------------------|
| Sign Out (logout) | No confirmation modal — direct action. Session cleared immediately on click. |

---

## Layout Grid

### Dashboard Layout

```
[Sidebar 260px fixed] [Main content area — flex-1]
                       [Topbar 64px fixed-top]
                       [Page content — padding 24px, overflow-y auto]
```

- Sidebar: `position: fixed`, `top: 0`, `left: 0`, `height: 100vh`, `width: 260px`, `z-index: 40`
- Main area: `margin-left: 260px`, `min-height: 100vh`
- Topbar: `position: sticky`, `top: 0`, `height: 64px`, `z-index: 30`, `width: 100%`
- Page content: `padding: 24px`, starts below topbar

### Stat Cards Grid

```css
display: grid;
grid-template-columns: repeat(4, 1fr);
gap: 16px;
```

Responsive breakpoints:
- `lg` (1024px+): 4 columns
- `md` (768px–1023px): 2 columns
- `sm` (below 768px): 1 column

### Charts Grid (below stat cards)

```css
display: grid;
grid-template-columns: 2fr 1fr;
gap: 16px;
margin-top: 16px;
```

Bar chart takes 2fr width. Donut chart takes 1fr.

---

## Interaction States

### All Interactive Elements

| State | Visual |
|-------|--------|
| Default | As specified per component |
| Hover | Background lightens by `--bg-border` overlay; cursor pointer |
| Focus | 2px outline `--accent-teal`, offset 2px — applies to all keyboard-focusable elements |
| Active/pressed | Background darkens 10% from hover |
| Disabled | Opacity 0.4, cursor not-allowed |
| Loading | Spinner (Lucide `Loader2` with animate-spin) replaces icon on buttons |

### Form Input States

| State | Border color | Background |
|-------|-------------|-----------|
| Default | `--bg-border` | `--bg-primary` |
| Focus | `--accent-teal` (2px) | `--bg-primary` |
| Error | `--status-red` | `--bg-primary` |
| Disabled | `--bg-border` at 50% | `--bg-surface` |

### Sign In Button States

| State | Appearance |
|-------|-----------|
| Default | `--accent-primary` background, `--text-primary` text |
| Hover | `--accent-primary` at 90% brightness |
| Loading | Spinner icon left of "Signing in..." text; button disabled |
| Disabled | Opacity 0.4 |

---

## Accessibility Contract

- All interactive elements: minimum 44px tap target (height or padding compensates for smaller visual size)
- Color contrast: `--text-primary` (#E6EDF3) on `--bg-surface` (#161B22) = 10.8:1 (WCAG AAA)
- Color contrast: `--text-muted` (#7D8590) on `--bg-surface` (#161B22) = 4.6:1 (WCAG AA)
- Color contrast: `--text-primary` on `--bg-primary` (#0D1117) = 12.5:1 (WCAG AAA)
- Focus rings: visible on all keyboard-navigable elements (`--accent-teal`, 2px, offset 2px)
- Screen reader: sidebar nav items use `aria-current="page"` on active item
- Language toggle: `aria-label="Switch language"`, `role="switch"`
- Notification bell: `aria-label="Notifications"` (EN) / `aria-label="Pemberitahuan"` (BM)
- Images: MyKasih logo has `alt="MyKasih Foundation"` on login, `alt=""` (decorative) in sidebar
- Stat cards: `role="region"` with `aria-label` matching the card label
- Tables: `<table>` with proper `<thead>`, `<tbody>`, `<th scope="col">` for each column header
- Skeleton loaders: `aria-busy="true"` on the container while loading

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Card, Dialog, Select, Tabs, Switch, Avatar, Badge, Checkbox, Dropdown-menu, Label, Separator, Skeleton, Sonner, Table | Not required — official registry |

No third-party registries declared. All components from `mykasih-crm/components/ui/` (already installed) or built as custom components in `mykasih-crm/components/`.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| All 12 CSS color tokens | CLAUDE.md — locked |
| Typography (Inter + JetBrains Mono, weight palette) | CLAUDE.md — locked |
| Shadcn initialization (default preset, slate base, CSS vars) | components.json — detected |
| Sidebar width 260px, all 14 nav items, 5 groups | CLAUDE.md DASH-01 + REQUIREMENTS.md |
| Channel badge colors (voice #43A047 / chat #00897B) | CLAUDE.md — locked |
| Topbar contents (title, date, lang toggle, search, bell, avatar) | REQUIREMENTS.md DASH-03, DASH-04 |
| Stat card labels (EN + BM, 4 cards) | REQUIREMENTS.md DASH-05 |
| Chart types (stacked bar Recharts + donut Recharts) | REQUIREMENTS.md DASH-06, DASH-07 |
| Recent interactions table columns and channel badge rule | REQUIREMENTS.md DASH-08 |
| Skeleton + empty state requirement | REQUIREMENTS.md DASH-09 |
| Role-based redirect routes | REQUIREMENTS.md AUTH-02 |
| Login error message placement | REQUIREMENTS.md AUTH-03 |
| No src/ directory, App Router, Next.js 16.x | CONTEXT.md D-07, code_context |
| Tailwind 4 syntax | CONTEXT.md code_context — confirmed in globals.css |
| Seed data convention (DEMO_ prefix) | CONTEXT.md D-12 |
| globals.css needs full rewrite | CONTEXT.md code_context |

---

## Revision Log

| Date | Change | Reason |
|------|--------|--------|
| 2026-04-11 | Collapsed 7 font sizes to 4 (28px, 16px, 14px, 12px) | Checker: max 4 sizes |
| 2026-04-11 | Removed weight 700 (Bold); palette is now 400 + 600 only | Checker: max 2 weights |
| 2026-04-11 | Badge padding changed from 2px/3px to 4px 8px (all badge types) | Checker: multiples of 4 only |
| 2026-04-11 | Added aria-label to notification bell in Topbar spec | Checker: icon-only elements require accessible label |
| 2026-04-11 | Sidebar nav icon margin-right changed from 10px to 8px | Checker: 10px is not on the declared spacing scale (multiples of 4) |
| 2026-04-11 | Added focal point statements to Login Page and Dashboard Home sections | Checker: non-blocking recommendation — clarifies visual hierarchy intent |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 01-scaffold-db-auth-dashboard-shell*
*UI-SPEC created: 2026-04-11*
*UI-SPEC revised: 2026-04-11*
*Consumed by: gsd-ui-checker, gsd-planner, gsd-executor, gsd-ui-auditor*
