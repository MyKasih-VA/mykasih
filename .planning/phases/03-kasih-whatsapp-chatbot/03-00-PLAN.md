---
phase: 03-kasih-whatsapp-chatbot
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mykasih-crm/package.json
  - mykasih-crm/__tests__/lib/meta-wa.test.ts
  - mykasih-crm/__tests__/lib/intent-classifier.test.ts
  - mykasih-crm/__tests__/lib/session-manager.test.ts
  - mykasih-crm/__tests__/lib/faq-handler.test.ts
  - mykasih-crm/__tests__/lib/balance-handler.test.ts
  - mykasih-crm/__tests__/lib/merchant-handler.test.ts
  - mykasih-crm/__tests__/lib/complaint-handler.test.ts
  - mykasih-crm/__tests__/api/webhook-chat.test.ts
  - mykasih-crm/__tests__/api/chatbot-message.test.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - CHAT-07
  - CHAT-08
  - CHAT-09
  - CHAT-10

must_haves:
  truths:
    - "@anthropic-ai/sdk is installed and importable"
    - "All 9 test stub files exist and pass (with --passWithNoTests or describe blocks)"
    - "Jest test run completes without configuration errors"
  artifacts:
    - path: "mykasih-crm/package.json"
      provides: "@anthropic-ai/sdk dependency"
      contains: "@anthropic-ai/sdk"
    - path: "mykasih-crm/__tests__/lib/meta-wa.test.ts"
      provides: "Test stubs for CHAT-08, CHAT-10"
    - path: "mykasih-crm/__tests__/lib/intent-classifier.test.ts"
      provides: "Test stubs for CHAT-03"
    - path: "mykasih-crm/__tests__/api/webhook-chat.test.ts"
      provides: "Test stubs for CHAT-01, CHAT-02"
    - path: "mykasih-crm/__tests__/api/chatbot-message.test.ts"
      provides: "Test stubs for CHAT-03 dispatch"
  key_links: []
---

<objective>
Install @anthropic-ai/sdk and create all Phase 3 test stub files so that subsequent plans have automated verification from day one.

Purpose: Wave 0 establishes the test scaffold and installs the only new npm dependency. Every subsequent plan can run `npm test` against real test files.
Output: package.json updated, 9 test files with describe blocks and placeholder tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kasih-whatsapp-chatbot/03-CONTEXT.md
@.planning/phases/03-kasih-whatsapp-chatbot/03-RESEARCH.md
@.planning/phases/03-kasih-whatsapp-chatbot/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk and create test stubs for lib modules</name>
  <files>
    mykasih-crm/package.json,
    mykasih-crm/__tests__/lib/meta-wa.test.ts,
    mykasih-crm/__tests__/lib/intent-classifier.test.ts,
    mykasih-crm/__tests__/lib/session-manager.test.ts,
    mykasih-crm/__tests__/lib/faq-handler.test.ts,
    mykasih-crm/__tests__/lib/balance-handler.test.ts,
    mykasih-crm/__tests__/lib/merchant-handler.test.ts,
    mykasih-crm/__tests__/lib/complaint-handler.test.ts
  </files>
  <read_first>
    mykasih-crm/package.json,
    mykasih-crm/jest.config.ts,
    mykasih-crm/__tests__/lib/ic-mask.test.ts
  </read_first>
  <action>
1. Run `cd mykasih-crm && npm install @anthropic-ai/sdk@0.88.0` to add the Anthropic SDK.
2. Verify package.json now contains `"@anthropic-ai/sdk"`.
3. Create 7 test stub files in `__tests__/lib/`. Each file should follow the existing pattern from `__tests__/lib/ic-mask.test.ts`. Each file contains a `describe()` block with `test.todo()` placeholders for the behaviors that will be implemented in later plans:

**__tests__/lib/meta-wa.test.ts** (CHAT-08, CHAT-10):
- `describe('sendWhatsAppMessage')` with todos: "logs to console when env vars absent", "calls fetch with correct URL when env vars present"
- `describe('sendWhatsAppButtons')` with todos: "uses type list not button", "sends 4 items in sections.rows array", "logs to console in stub mode"

**__tests__/lib/intent-classifier.test.ts** (CHAT-03):
- `describe('classifyIntent')` with todos: "returns balance_check for semak baki", "returns faq for apa tu sara", "returns merchant_lookup for kedai berdekatan", "returns complaint for ada masalah", "returns unknown with low confidence on parse error", "detects bm language for BM messages", "detects en language for English messages"

**__tests__/lib/session-manager.test.ts**:
- `describe('getActiveSession')` with todos: "returns null when no active session", "returns most recent non-expired session", "ignores expired sessions"
- `describe('createSession')` with todos: "creates session with wa_phone and language", "sets step to 0 and empty collected_data"
- `describe('updateSession')` with todos: "updates intent and step", "merges collected_data"
- `describe('expireSession')` with todos: "sets expires_at to now"

**__tests__/lib/faq-handler.test.ts** (CHAT-04):
- `describe('faqHandler')` with todos: "queries kb_entries with is_active true", "returns answer_bm for bm language session", "returns answer_en for en language session", "returns fallback when no kb entry found"

**__tests__/lib/balance-handler.test.ts** (CHAT-05):
- `describe('balanceHandler')` with todos: "step 0 asks for IC number", "step 1 masks IC before storing in session", "step 1 never stores raw IC in collected_data", "step 1 returns balance with expiry and nearest merchant", "calls mockBalanceAPI with masked IC"

**__tests__/lib/merchant-handler.test.ts** (CHAT-06):
- `describe('merchantHandler')` with todos: "step 0 asks for postcode or location", "step 1 calls lookupByPostcode for numeric input", "step 1 calls lookupByState for text input", "returns top 3-5 merchants formatted", "saves call with category merchant_lookup"

**__tests__/lib/complaint-handler.test.ts** (CHAT-07, CHAT-09):
- `describe('complaintHandler')` with todos: "step 0 asks for name", "step 1 asks for IC", "step 2 masks IC immediately", "step 3 asks to confirm details", "step 4 creates ticket with TKT ref on confirmation", "step 4 saves call with channel chat", "step 4 cancels on rejection", "inserts call with channel=chat and category=complaint"
  </action>
  <verify>
    <automated>cd mykasih-crm && npm test -- --testPathPattern="(meta-wa|intent-classifier|session-manager|faq-handler|balance-handler|merchant-handler|complaint-handler)" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@anthropic-ai/sdk": "0.88.0"` or `"@anthropic-ai/sdk": "^0.88.0"`
    - node_modules/@anthropic-ai/sdk/package.json exists
    - __tests__/lib/meta-wa.test.ts contains `describe('sendWhatsAppMessage'`
    - __tests__/lib/meta-wa.test.ts contains `describe('sendWhatsAppButtons'`
    - __tests__/lib/intent-classifier.test.ts contains `describe('classifyIntent'`
    - __tests__/lib/session-manager.test.ts contains `describe('getActiveSession'`
    - __tests__/lib/faq-handler.test.ts contains `describe('faqHandler'`
    - __tests__/lib/balance-handler.test.ts contains `describe('balanceHandler'`
    - __tests__/lib/merchant-handler.test.ts contains `describe('merchantHandler'`
    - __tests__/lib/complaint-handler.test.ts contains `describe('complaintHandler'`
  </acceptance_criteria>
  <done>All 7 lib test stubs created with describe blocks and todo placeholders. @anthropic-ai/sdk installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for API routes</name>
  <files>
    mykasih-crm/__tests__/api/webhook-chat.test.ts,
    mykasih-crm/__tests__/api/chatbot-message.test.ts
  </files>
  <read_first>
    mykasih-crm/__tests__/api/webhook-voice.test.ts,
    mykasih-crm/jest.config.ts
  </read_first>
  <action>
Create 2 API route test stub files following the pattern from `__tests__/api/webhook-voice.test.ts`:

**__tests__/api/webhook-chat.test.ts** (CHAT-01, CHAT-02):
- `describe('GET /api/webhook/chat')` with todos: "returns hub.challenge when mode=subscribe and token matches", "returns 403 when token does not match", "returns 403 when mode is not subscribe", "returns 403 when hub.challenge is missing"
- `describe('POST /api/webhook/chat')` with todos: "returns 401 when n8n secret is missing", "returns 401 when n8n secret is wrong", "extracts text message body from Meta payload", "extracts button_reply title from interactive payload", "returns 200 for status-only webhook events (no messages array)", "deduplicates on wa_message_id (WAMID)"

**__tests__/api/chatbot-message.test.ts** (CHAT-03):
- `describe('POST /api/chatbot/message')` with todos: "returns 401 without n8n webhook secret", "classifies intent and dispatches to correct handler", "sends first contact list on new session", "uses locked session intent instead of re-classifying", "returns 500 on unhandled error"
  </action>
  <verify>
    <automated>cd mykasih-crm && npm test -- --testPathPattern="(webhook-chat|chatbot-message)" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/api/webhook-chat.test.ts contains `describe('GET /api/webhook/chat'`
    - __tests__/api/webhook-chat.test.ts contains `describe('POST /api/webhook/chat'`
    - __tests__/api/webhook-chat.test.ts contains `wa_message_id` or `WAMID`
    - __tests__/api/chatbot-message.test.ts contains `describe('POST /api/chatbot/message'`
    - __tests__/api/chatbot-message.test.ts contains `first contact`
    - npm test exits 0
  </acceptance_criteria>
  <done>Both API route test stubs exist with describe blocks covering all CHAT-01 through CHAT-03 behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 is test scaffolding only — no production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-00-01 | Information Disclosure | package.json | accept | No secrets in package.json; @anthropic-ai/sdk is a public npm package |
</threat_model>

<verification>
cd mykasih-crm && npm test -- --passWithNoTests
All 9 new test files discovered and pass (todos are not failures).
</verification>

<success_criteria>
- @anthropic-ai/sdk 0.88.0 installed in mykasih-crm/node_modules
- 9 test stub files exist in __tests__/ with correct describe blocks
- `npm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-kasih-whatsapp-chatbot/03-00-SUMMARY.md`
</output>
